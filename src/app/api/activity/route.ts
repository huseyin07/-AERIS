import {NextResponse} from "next/server";import {ARC} from "@/data/arc";import {getRecentActivity} from "@/data/arc-source";export const dynamic="force-dynamic";export async function GET(){try{const d=await getRecentActivity();return NextResponse.json({network:ARC.name,chainId:ARC.chainId,latestBlock:d.latestBlock.toString(),transfers:d.transfers,fetchedAt:Date.now()},{headers:{"Cache-Control":"no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Arc RPC unavailable"},{status:503})}}
