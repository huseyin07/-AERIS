"use client";import dynamic from "next/dynamic";import {LiveActivity} from "./live-activity";import {useActivity} from "@/state/activity-store";import {money,short} from "@/lib/format";import {ARC} from "@/data/arc";const NetworkScene=dynamic(()=>import("@/visualization/network-scene").then(m=>m.NetworkScene),{ssr:false});export function Dashboard(){const t=useActivity(s=>s.transfers),status=useActivity(s=>s.connection),volume=t.reduce((a,x)=>a+Number(x.value),0),addresses=new Set(t.flatMap(x=>[x.from,x.to])).size;return <main><LiveActivity/><header><b>AERIS</b><nav>LIVE <span>EXPLORE</span><span>ABOUT</span></nav><div className={"status "+status}>{status.toUpperCase()} · ARC TESTNET</div></header><section className="hero"><div className="copy"><p className="eyebrow">ARC ECONOMY OBSERVATORY</p><h1>Watch money move.</h1><p>A cinematic, real-time view of USDC activity on Arc.</p></div><div className="scene"><NetworkScene transfers={t}/></div><div className="metrics"><div><small>USDC FLOW</small><strong>{"$"+money(String(volume))}</strong></div><div><small>TRANSFERS</small><strong>{t.length}</strong></div><div><small>ACTIVE ADDRESSES</small><strong>{addresses}</strong></div></div></section><section className="feed"><div className="feedHead"><h2>Live transfers</h2><span>Real onchain data · {ARC.name}</span></div>{t.slice().reverse().slice(0,18).map(x=><a key={x.id} href={ARC.explorer+"/tx/"+x.txHash} target="_blank" rel="noreferrer"><span>{short(x.from)} → {short(x.to)}</span><b>{money(x.value)} USDC</b><em>#{x.blockNumber}</em></a>)}{!t.length&&<p className="empty">Waiting for verified Arc USDC transfers…</p>}</section><footer>Mainnet values are not guessed. AERIS will switch when official Arc Mainnet developer configuration is published.</footer></main>}
