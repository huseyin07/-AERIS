"use client";import dynamic from "next/dynamic";import {LiveActivity} from "./live-activity";import {useActivity} from "@/state/activity-store";import {money,short} from "@/lib/format";import {ARC} from "@/data/arc";const NetworkScene=dynamic(()=>import("@/visualization/network-scene").then(m=>m.NetworkScene),{ssr:false});export function Dashboard(){const t=useActivity(s=>s.transfers),status=useActivity(s=>s.connection),selected=useActivity(s=>s.selected),select=useActivity(s=>s.select),query=useActivity(s=>s.query),setQuery=useActivity(s=>s.setQuery);const volume=t.reduce((a,x)=>a+Number(x.value),0),addresses=new Set(t.flatMap(x=>[x.from,x.to])).size,contracts=new Set(t.flatMap(x=>[[x.from,x.fromType],[x.to,x.toType]] as const).filter(x=>x[1]==="contract").map(x=>x[0])).size,largest=t.reduce((m,x)=>Number(x.value)>Number(m?.value??0)?x:m,t[0]),filtered=query?t.filter(x=>x.txHash.toLowerCase().includes(query.toLowerCase())||x.from.toLowerCase().includes(query.toLowerCase())||x.to.toLowerCase().includes(query.toLowerCase())):t,related=selected?t.filter(x=>x.from===selected||x.to===selected).slice(-6).reverse():[];return <main><LiveActivity/><header><div className="brand"><i/>AERIS</div><nav><b>LIVE</b><span>EXPLORE</span><span>ABOUT</span></nav><input className="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Address or transaction"/><div className={"status "+status}><i/>{status.toUpperCase()} · ARC MAINNET</div></header><section className="hero"><div className="intro"><span>ARC ECONOMY · LIVE</span><p>Verified USDC movement across Arc Mainnet.</p></div><div className="scene"><NetworkScene transfers={t} selected={selected} onSelect={select}/></div><div className="legend"><span><i/>WALLET</span><span><i className="contract"/>CONTRACT</span><span><i className="flow"/>USDC FLOW</span></div><div className="metrics"><div><small>USDC FLOW</small><strong>{"$"+money(String(volume))}</strong></div><div><small>TRANSFERS</small><strong>{t.length}</strong></div><div><small>ACTIVE ENTITIES</small><strong>{addresses}</strong></div><div><small>CONTRACTS</small><strong>{contracts}</strong></div><div><small>LARGEST</small><strong>{largest?money(largest.value)+" USDC":"—"}</strong></div></div>{selected&&<aside><button onClick={()=>select(null)}>×</button><small>SELECTED ENTITY</small><h3>{short(selected)}</h3><p>{selected}</p><div className="asideStats"><span>RECENT FLOW</span><b>{related.length} transfers</b></div>{related.map(x=><a key={x.id} href={ARC.explorer+"/tx/"+x.txHash} target="_blank" rel="noreferrer">{short(x.from)} → {short(x.to)} <b>{money(x.value)}</b></a>)}</aside>}</section><section className="timebar"><div><b>LIVE</b><span>1H</span><span>24H</span><span>7D</span><span>30D</span></div><em>ARC MAINNET · VERIFIED ONCHAIN</em></section><section className="feed"><div className="feedHead"><div><small>NETWORK ACTIVITY</small><h2>Latest USDC transfers</h2></div><span>Streaming from {ARC.name}</span></div>{filtered.slice().reverse().slice(0,16).map(x=><a key={x.id} href={ARC.explorer+"/tx/"+x.txHash} target="_blank" rel="noreferrer"><i/><span>{short(x.from)} <em>→</em> {short(x.to)}</span><b>{money(x.value)} USDC</b><small>BLOCK {x.blockNumber}</small></a>)}{!filtered.length&&<p className="empty">Waiting for verified Arc Mainnet activity…</p>}</section><footer><b>AERIS</b><span>Arc Mainnet observatory</span></footer></main>}